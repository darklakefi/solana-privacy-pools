{
  "name": "solana-privacy-pools-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Solana Privacy Pools",
  "main": "index.js",
  "scripts": {
    "pretest": "node setup.js",
    "test": "mocha e2e/**/*.test.js --timeout 10000",
    "test:watch": "mocha e2e/**/*.test.js --watch --timeout 10000",
    "test:unit": "cargo test",
    "test:all": "npm run test:unit && npm run test"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/web3.js": "^1.87.0",
    "borsh": "^0.7.0",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.0",
    "bigint-buffer": "^1.1.5"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "@litesvm/node": "^0.2.0"
  }
}